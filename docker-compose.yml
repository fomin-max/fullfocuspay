services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ffp
      POSTGRES_PASSWORD: ffp
      POSTGRES_DB: ffp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
